on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ls -lah
      - run: echo "hello world"
  build-2:
    runs-on: ubuntu-latest
    steps:
      - run: ls -lah
  build-3:
    runs-on: ubuntu-latest
    needs: build-2
    steps:
      - uses: actions/checkout@v6
      - run: npm install
      - run: npm run build